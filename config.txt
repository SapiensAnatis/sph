# Somewhat proprietary config format:
# Lines starting with a '#' (like this one!) aren't parsed
# Lines should be structured as 'PROPERTYNAME VALUE' (i.e. separated by 1 space)
# See readme.txt for the full list of parameters that will be looked for

# Number of particles to create at program start.
n_part 10

# The units of distance, in m, that the code will use.
d_unit 1
# The units of time, in seconds, that the code will use.
t_unit 3e6

# The size of the boundary which extends either side of the origin, in units of d_unit. 
limit 1